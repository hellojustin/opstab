<% content_for :content do %>

  <div class="span4">
    <%= render 'layouts/user_sidebar' %>
  </div>

  <div class="span8">
    <%= render 'layouts/messages' %>
    <%= content_for?( :user_content ) ? yield( :user_content ) : yield %>
  </div>

<% end %>

<%= render :template => 'layouts/application' %>
